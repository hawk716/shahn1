# دليل تثبيت وتشغيل بوت التيليجرام

## المتطلبات
- Python 3.10+
- pip

## خطوات التثبيت

### 1. نسخ متغيرات البيئة
```bash
cp bot/.env.example bot/.env
```

### 2. ملء بيانات التيليجرام
```bash
# حرر bot/.env وأضف:
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
TELEGRAM_ADMIN_CHAT_ID=YOUR_ADMIN_CHAT_ID_HERE
```

### 3. تثبيت المكتبات
```bash
cd bot
pip install -r requirements.txt
```

### 4. تشغيل البوت
```bash
python -m main
```

## الحصول على البيانات المطلوبة

### الحصول على BOT_TOKEN
1. تكلم مع [@BotFather](https://t.me/BotFather)
2. أرسل `/newbot`
3. اتبع التعليمات واحصل على التوكن

### الحصول على ADMIN_CHAT_ID
1. أرسل رسالة عادية للبوت
2. افتح: `https://api.telegram.org/bot{TOKEN}/getUpdates`
3. ابحث عن `chat.id` في الرد

## أوامر البوت

- `/start` - البدء
- `/admin {user_id}` - إضافة مشرف جديد (للمشرف الرئيسي فقط)
- زر "المعاملات 📋" - عرض آخر 10 معاملات

## هيكل قاعدة البيانات

يتم حفظ المشرفين وطلبات السحب في `bot_data.db` (SQLite)

## ربط البوت مع الموقع

عند قبول طلب سحب من الموقع:
1. يتم إرسال POST request إلى `/api/telegram/webhook`
2. يقوم الـ API بإرسال رسالة للبوت
3. يتلقى المشرفون الطلب مع أزرار التفاعل

## نظام الأزرار

### المرحلة الأولى
- "قبول 🟢" - قبول الطلب
- "مراسلة 📧" - عرض بريد العميل

### المرحلة الثانية (بعد القبول)
- "موافقة ✅" (أخضر) - الموافقة على السحب
- "رفض ❌" (أحمر) - رفض السحب
